interface BookEditProps {
  // Define your props here, for example:
  // title: string;
}

export default function BookEdit(props: BookEditProps) {
  return (
    <div>BookEditprops: BookEditProps</div>
  )
}
